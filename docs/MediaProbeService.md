# MediaProbeService

The `MediaProbeService` is a utility service in the `ffmpeg-dotnet-wrapper` project that uses `ffprobe` to analyze media files and extract metadata such as format details, stream information, duration, bitrate, and codec properties. It is designed to provide structured, type-safe access to media file properties for applications requiring media analysis without decoding the actual media data.

## API

### `Probe`
